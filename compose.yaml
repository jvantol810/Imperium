services:
  listmaster:
    build:
      context: .
      dockerfile: Dockerfile
    image: listmaster:latest
    ports:
      - "8090:3000"
    develop:
      watch:
        - action: sync
          path: .
          target: /src